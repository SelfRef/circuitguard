services:
  circuitguard:
    image: ghcr.io/selfref/circuitguard:latest
    env_file: .env
    environment:
      DB_PATH: /data/circuitguard.db
    volumes:
      - circuitguard-data:/data
    restart: unless-stopped

volumes:
  circuitguard-data:
